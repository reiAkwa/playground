#include <bits/stdc++.h>

int solve(int t) {
  int s = 0;
  if (t <= 3500) {
    s = t;
  }

  return s;
}

int wage(int s) {
  int a = s - 3500;
  int tax = 0;
  if (s <= 3500) {
    return s;
  } else {
    tax += 
  }
}

int main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);

  int t = 0;
  std::cin >> t;

  int s = 0;
}
